/**
 * WebRTC Meeting Flow - Step by Step
 *
 * This diagram explains how two users (Alice & Bob) establish a video call
 * using WebRTC with Socket.IO signaling.
 */

// ==================== STEP 1: ALICE JOINS MEETING ====================
/*
Alice clicks "Join Meeting" button
┌─────────────────────────────────────────────────────────────────┐
│ 1. Alice's Browser                                              │
│    - Requests camera/microphone access                          │
│    - getUserMedia() → localStream                               │
│    - Emits: socket.emit("join-meeting", { meetingId, userId })  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. Server (Socket.IO)                                           │
│    - Alice joins room                                           │
│    - Server tracks: activeMeetings[meetingId] = [Alice]         │
│    - Emits: socket.emit("existing-participants", [])            │
│    - (No existing participants yet)                             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. Alice's Browser                                              │
│    - Receives empty participants list                           │
│    - Displays local video only                                  │
│    - Waits for others to join                                   │
└─────────────────────────────────────────────────────────────────┘
*/

// ==================== STEP 2: BOB JOINS MEETING ====================
/*
Bob clicks "Join Meeting" button (same projectId)
┌─────────────────────────────────────────────────────────────────┐
│ 1. Bob's Browser                                                │
│    - Requests camera/microphone access                          │
│    - getUserMedia() → localStream                               │
│    - Emits: socket.emit("join-meeting", { meetingId, userId })  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. Server (Socket.IO)                                           │
│    - Bob joins room                                             │
│    - Server tracks: activeMeetings[meetingId] = [Alice, Bob]    │
│    - Emits to Bob: "existing-participants" [Alice]              │
│    - Emits to Alice: "user-joined" { socketId: Bob }            │
└─────────────────────────────────────────────────────────────────┘
*/

// ==================== STEP 3: WEBRTC OFFER (Bob → Alice) ====================
/*
Bob creates an offer to connect with Alice
┌─────────────────────────────────────────────────────────────────┐
│ 1. Bob's Browser                                                │
│    - Creates RTCPeerConnection for Alice                        │
│    - Adds local stream tracks to connection                     │
│    - peerConnection.createOffer()                               │
│    - peerConnection.setLocalDescription(offer)                  │
│    - Emits: socket.emit("offer", { offer, to: Alice })          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. Server (Socket.IO)                                           │
│    - Receives offer from Bob                                    │
│    - Forwards to Alice: io.to(Alice).emit("offer", { offer })   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. Alice's Browser                                              │
│    - Receives offer from Bob                                    │
│    - Creates RTCPeerConnection for Bob                          │
│    - peerConnection.setRemoteDescription(offer)                 │
└─────────────────────────────────────────────────────────────────┘
*/

// ==================== STEP 4: WEBRTC ANSWER (Alice → Bob) ====================
/*
Alice creates an answer in response to Bob's offer
┌─────────────────────────────────────────────────────────────────┐
│ 1. Alice's Browser                                              │
│    - peerConnection.createAnswer()                              │
│    - peerConnection.setLocalDescription(answer)                 │
│    - Emits: socket.emit("answer", { answer, to: Bob })          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. Server (Socket.IO)                                           │
│    - Receives answer from Alice                                 │
│    - Forwards to Bob: io.to(Bob).emit("answer", { answer })     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. Bob's Browser                                                │
│    - Receives answer from Alice                                 │
│    - peerConnection.setRemoteDescription(answer)                │
└─────────────────────────────────────────────────────────────────┘
*/

// ==================== STEP 5: ICE CANDIDATE EXCHANGE ====================
/*
Both browsers exchange network information (ICE candidates)
This happens AUTOMATICALLY during offer/answer exchange

┌─────────────────────────────────────────────────────────────────┐
│ Both Alice & Bob                                                │
│    - peerConnection.onicecandidate event fires multiple times   │
│    - Each candidate is sent via Socket.IO                       │
│                                                                  │
│    Alice:                                                        │
│    - Emits: socket.emit("ice-candidate", { candidate, to: Bob })│
│                                                                  │
│    Bob:                                                          │
│    - Emits: socket.emit("ice-candidate", { candidate, to: Alice })│
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ Server (Socket.IO)                                              │
│    - Forwards each ICE candidate to the intended peer           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ Both Alice & Bob                                                │
│    - Receive ICE candidates from peer                           │
│    - peerConnection.addIceCandidate(candidate)                  │
│    - RTCPeerConnection tries different network paths            │
└─────────────────────────────────────────────────────────────────┘
*/

// ==================== STEP 6: PEER-TO-PEER CONNECTION ESTABLISHED ====================
/*
Direct connection established between browsers
┌─────────────────────────────────────────────────────────────────┐
│ Connection State Changes                                        │
│    - "connecting" → "connected" → "stable"                      │
│                                                                  │
│ Media Streams Flow                                              │
│    Alice ──────────────────────────────► Bob                   │
│         ◄────────────────────────────── (peer-to-peer)          │
│                                                                  │
│ peerConnection.ontrack fires                                    │
│    - Alice receives Bob's video stream                          │
│    - Bob receives Alice's video stream                          │
│                                                                  │
│ Displayed in UI                                                 │
│    - setRemoteStreams(stream)                                   │
│    - <video> elements show remote participants                  │
└─────────────────────────────────────────────────────────────────┘
*/

// ==================== STEP 7: ONGOING MEETING ====================
/*
Meeting in progress - all communication is peer-to-peer
┌─────────────────────────────────────────────────────────────────┐
│ Alice's Browser                     Bob's Browser               │
│ ┌────────────────┐                  ┌────────────────┐         │
│ │  Local Video   │ ───WebRTC──────► │ Remote Video   │         │
│ │  (Alice)       │                  │ (Alice)        │         │
│ └────────────────┘                  └────────────────┘         │
│                                                                  │
│ ┌────────────────┐                  ┌────────────────┐         │
│ │ Remote Video   │ ◄───WebRTC────── │  Local Video   │         │
│ │  (Bob)         │                  │  (Bob)         │         │
│ └────────────────┘                  └────────────────┘         │
│                                                                  │
│ Audio/Video Controls                Audio/Video Controls        │
│ [🎤] [📹] [📞]                      [🎤] [📹] [📞]              │
└─────────────────────────────────────────────────────────────────┘

Server is NOT involved in media streaming!
Server only used for:
- Join/leave notifications
- New participant signaling
*/

// ==================== STEP 8: BOB LEAVES MEETING ====================
/*
Bob clicks "Leave Meeting"
┌─────────────────────────────────────────────────────────────────┐
│ 1. Bob's Browser                                                │
│    - Stops local stream: localStream.getTracks().stop()        │
│    - Closes peer connections: peerConnection.close()            │
│    - Emits: socket.emit("leave-meeting")                        │
│    - Navigates away from meeting page                           │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. Server (Socket.IO)                                           │
│    - Removes Bob from activeMeetings[meetingId]                 │
│    - Emits to Alice: "user-left" { socketId: Bob }              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. Alice's Browser                                              │
│    - Receives "user-left" event                                 │
│    - Removes Bob from participants list                         │
│    - Removes Bob's video from UI                                │
│    - Closes peer connection with Bob                            │
│    - Alice continues alone in meeting                           │
└─────────────────────────────────────────────────────────────────┘
*/

// ==================== KEY WEBRTC OBJECTS ====================
/*
RTCPeerConnection
├── Purpose: Manages peer-to-peer connection
├── Methods:
│   ├── createOffer()         - Initiate connection
│   ├── createAnswer()        - Respond to offer
│   ├── setLocalDescription() - Set own SDP
│   ├── setRemoteDescription()- Set peer's SDP
│   ├── addIceCandidate()    - Add network candidate
│   └── addTrack()           - Add media track
└── Events:
    ├── onicecandidate       - ICE candidate found
    ├── ontrack              - Remote track received
    └── onconnectionstatechange - Connection status

RTCSessionDescription (SDP)
├── type: "offer" or "answer"
└── sdp: Session Description (codec, bandwidth, etc.)

RTCIceCandidate
├── candidate: Network path information
├── sdpMid: Media stream ID
└── sdpMLineIndex: Media line index
*/

// ==================== IMPORTANT NOTES ====================
/*
1. STUN Server (stun.l.google.com:19302)
   - Helps discover public IP address
   - Required for NAT traversal
   - Free Google-provided service

2. TURN Server (Not Implemented Yet)
   - Relays media when peer-to-peer fails
   - Required for restrictive firewalls
   - Costs money (bandwidth usage)
   - Recommended for production

3. Socket.IO vs WebRTC
   - Socket.IO: Signaling only (setup)
   - WebRTC: Actual media streaming
   - Media NEVER goes through server

4. Multiple Participants
   - Each user has peer connection with EVERY other user
   - 3 users = 3 connections each (mesh topology)
   - 10 users = 45 total connections (not scalable)
   - For large meetings, use SFU (Selective Forwarding Unit)

5. Browser Compatibility
   - Chrome/Edge: Full support
   - Firefox: Full support
   - Safari: Full support (iOS 11+)
   - Mobile: Limited (check browser API support)
*/
